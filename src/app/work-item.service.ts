export class WorkItemService {
  /**
   * This service will have all the work items fetched from the backend. It can then be injected by other components
   */
  workItems = [];

  constructor() {

   }
}
